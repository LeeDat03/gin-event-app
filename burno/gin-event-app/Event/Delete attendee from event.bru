meta {
  name: Delete attendee from event
  type: http
  seq: 8
}

delete {
  url: http://localhost:8000/api/v1/events/:id/attendees/:userId
  body: none
  auth: inherit
}

params:path {
  id: 15
  userId: 4
}

settings {
  encodeUrl: true
}
